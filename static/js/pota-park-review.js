(function () { "use strict"; window.initPotaParkReview = function () { document.querySelectorAll("[data-pota-park-map]").forEach(async function (element) { const controller = await window.RadioOutdoorsEditablePinMap.create({ mapElement: element, latInput: document.getElementById(element.dataset.latitudeInput), lngInput: document.getElementById(element.dataset.longitudeInput), removeButton: document.getElementById(element.dataset.removeButton), statusElement: document.getElementById(element.dataset.status), coordinateDisplay: document.getElementById(element.dataset.coordinates), pinColor: "#f28c28", pinGlyph: "P", pinTitle: "Approximate POTA park location", placedMessage: "Approximate park location—review or move this pin.", removedMessage: "Location not found—place pin by clicking the map." }); const candidate = element.closest("td").querySelector("[data-pota-candidate]"); if (candidate) candidate.addEventListener("change", function () { const option = candidate.options[candidate.selectedIndex]; controller.place({lat: option.dataset.latitude, lng: option.dataset.longitude}, true); }); }); }; }());
